name: Sync status.json from issues
on:
  workflow_dispatch:

  push:
    branches: [ "main" ]
    paths:
      - ".github/workflows/sync-status-json.yml"
      - "scripts/sync-incident.mjs"
      - "index.html"
      - "status.json"

  issues:
    types: [opened, edited, reopened, closed]
