name: Sync Status JSON

on:
  issues:
    types: [opened, edited, deleted, closed, reopened]

jobs:
  update-json:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Necessário para o git pull --rebase funcionar

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Sync Script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_CONTEXT: ${{ toJson(github.event.issue) }}
          EVENT_ACTION: ${{ github.event.action }}
        # O erro acontecia aqui. Agora chamamos o arquivo externo:
        run: node scripts/sync-incident.mjs

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add status.json
          
          # Verifica se há mudanças antes de commitar
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update status.json from issues [skip ci]"
            git pull --rebase origin main
            git push origin main
          fi
